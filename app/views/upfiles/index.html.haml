%h1 Drobox

%hr

%table
  %tr
    %th ファイル名
    %th 種類
    %th 更新

  - upload_folder(@upfiles.first).sub_folders.each do |folder|
    %tr
      %td= link_to folder.name, folder_upfiles_path(folder)
      %td= file_type_name(folder)

  - @upfiles.each_with_index do |upfile, index|
    %tr
      %td= link_to upfile.name, ''


-#       %td= link_to 'Edit', edit_upfile_path(upfile)
-#       %td= link_to 'Destroy', upfile, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'アップロード', new_folder_upfile_path(upload_folder(@upfiles.first))
